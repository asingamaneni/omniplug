---
name: Bug report
about: Something omniplug does is wrong
labels: bug
---

**What happened?**

**What did you expect?**

**Reproduction**

```bash
# commands you ran (omniplug validate/build/install ...), plus the plugin
# source that triggers it (a minimal plugin.yaml / hooks.yaml / SKILL.md)
```

**Environment**

- omniplug version (`omniplug --version`):
- OS/arch:
- Install method (brew / npm / go install / binary):
- Target tool + version (Claude Code / Cursor), if the compiled output is the problem:
